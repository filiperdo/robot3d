$(".bseguir").click(function(){
	alert('oi2');
    /*$target = $(this);
      $liUser = '#li-user-' + $(this).attr('id');
      $($target).html('Seguindo...');
      $.post(URL+'follow/followUser/'+$(this).attr('id'), function(data){
          $($target).addClass('active');
          $($target).html(data);
          $($liUser).fadeOut( "slow", function() {
              $(this).remove();
          });
      });*/
});
